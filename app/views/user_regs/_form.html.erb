<%= form_with(model: user_reg) do |form| %>
  <% if user_reg.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(user_reg.errors.count, "error") %> prohibited this user_reg from being saved:</h2>

      <ul>
        <% user_reg.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

<div class="form-group">
    <%= form.label :events, style: "display: block" %><br>
    <%= form.text_field :articles,value: user_reg.articles.map(&:title).join(",") %>
  </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
